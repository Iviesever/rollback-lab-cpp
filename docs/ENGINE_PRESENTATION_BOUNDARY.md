# Engine presentation boundary

The SDK owns canonical player/projectile positions, velocities, HP, score, input history, predictions, snapshots, rollback decisions and hashes. UE owns key sampling, the external wall-clock accumulator, geometry, camera and HUD state.

```text
UE keys -> bounded button bits -> C ABI live step
                                      ↓
                       Existing Core sessions and packets
                                      ↓
                    Copied snapshots and correction results
                                      ↓
                       UE transforms, geometry and HUD
```

An Actor transform is a projection of integer coordinates. It is never simulation authority. Moving a visual object, changing camera projection or fading a correction ghost must not write position/velocity back to an SDK session. Rendering can use float/double; the canonical C++ state remains integer-only at 1/1,024-world-unit scale.

## Two worlds and one scene

In the seeded dual-view mode, the two visible arenas read two different session handles. Each handle owns its own canonical world, input history, snapshots and rollback decisions. The two worlds are displayed in one Unreal scene; they are not two UE Replication clients. The production live driver transfers versioned packets between the sessions. It cannot copy one canonical state into the other to make their hashes match.

Isolation is tested at several levels: advancing A leaves B unchanged, wrong/shared/used session pairs are rejected, session hashes and history are checked independently, and UE verifies distinct handles with real prediction and correction. Handle addresses are process-local diagnostics and do not enter scenario, replay, trace or report identity.

The native presentation Actor creates a fixed pool of two floor components, four player components and 128 projectile components, six shared material instances and one camera. Projectile visual slots are reused. The HUD uses a cached view reference instead of searching the world each tick. Map and material are generated by repository code, without manually placed Actors or Blueprint setup.

## One world per UDP process

The UDP mode creates exactly one SDK session per UE client and uses the same bounded presentation pool, hiding the unused peer view. Each visible world still contains both players. The remote player is predicted or corrected inside that local session from packets; no remote Actor or shared world is treated as authority. Group evidence checks distinct PIDs, actual bound ports, local peer identity and `session_count=1`.

The orthographic camera follows the selected local-view offset. Automatic target-plane recentering is disabled only for UDP, with manual near/far planes 0/2000, so the single world remains centered. This is a projection correction, with no change to state, replay or hash. The repeated real UDP run has five inspected images: start/convergence for both peers and a real correction on B. A had no local rollback in that schedule, so no correction image was invented.

## Correction and desync

Correction presentation consumes a real SDK correction's copied before/after worlds, earliest frame and resimulation depth. Its revision advances only after an actual correction. The renderer retains that event for a transient ghost, line and flash. Multiple corrections during catch-up still execute canonically; the display can show the most recently observed event while metrics/trace retain the actual run history.

A predicted difference concerns an unconfirmed state and is normal under latency. A desync is a hash mismatch for the same confirmed boundary. The controlled damage-bias demonstration reports the earliest confirmed divergent frame; it does not label normal prediction differences as failures.

Rollback replaces canonical effects by restoring and resimulating, rather than appending duplicate gameplay damage/projectiles. A production audio/VFX system would additionally need stable event identity and reconciliation. This demo's correction indicators are bounded presentation; it does not implement a general production event-reconciliation framework.

## Ownership on exit

The presentation Actor, `ARollbackArenaView`, stops its run and unregisters capture callbacks at EndPlay. The game-instance subsystem deinitializes the native Runtime; module pre-exit/shutdown stops registered runtimes. Native ownership destroys the borrowing live or UDP driver, then its owned session handle(s) inside the SDK, then releases the final DLL reference. Reset clears transient input and correction display state. Actor destruction cannot leave an SDK handle calling an unloaded library.

For the seeded path, the same scripted inputs and transport seeds pass through CLI, C ABI and UE, and complete report/trace/replay comparisons constrain the shared implementation path. Real UDP instead checks the agreed profile, equal confirmed hashes and equal peer replays through canonical reconstruction; counters and report identities depend on OS scheduling. Prediction and correction are required in aggregate, not necessarily on every client. Equal hashes do not prove collision freedom or absence of a shared bug. Golden sample bytes, compiler parity, canonical replay reconstruction, negative tests and ownership review add independent constraints. Final packaged proof is identified in the [verification matrix](../tasks/20260904-100800-ue5-live-integration-0.2/verification_matrix.md) and generated verifier JSON.
