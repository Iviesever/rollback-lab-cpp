# RollbackLab 0.2.0 candidate

The user requests an embedded C++23 rollback SDK, stable C ABI v1, UE 5.8 runtime plugin, interactive dual-peer arena, and packaged three-way hash-parity evidence in the existing repository. The user authorizes implementation, local locked dependencies, tests, commits, push, and a Draft PR, without another planning approval. Merge, tags, releases, system configuration changes, other repositories, parallel checkouts, and Engine modifications remain prohibited.

The complete acceptance objective is preserved in product_contract.md. Existing 0.1 contracts remain binding where 0.2 does not explicitly add engine integration. Historical task instructions to create repositories, use the old branch, or prohibit engine integration do not authorize actions or override this new objective.
